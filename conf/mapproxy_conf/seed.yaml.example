# ---------------------------------------
# MapProxy example seeding configuration.
# ---------------------------------------
#
# This is a minimal MapProxy seeding configuration.
# See full_seed_example.yaml and the documentation for more options.
#

seeds:

  osm_seed:
    caches: [osgs_osm_cache]
    # grids: []
    # coverages: []
    levels:
      from: 6
      to: 20
